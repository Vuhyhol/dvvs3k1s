<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <a href="/" class="back-button">← Back</a>
        <h1>Edit Post</h1>
        <form action="/edit/<%= post._id %>" method="POST">
            <input type="text" name="title" value="<%= post.title %>" placeholder="Post Title" required>
            <textarea name="description" rows="5" required><%= post.description %></textarea>
            <input type="text" name="author" value="<%= post.author %>" placeholder="Author Name" required>
            <button type="submit">Update Post</button>
        </form>
    </div>
</body>
</html>
